---
# Fixture: minimal valid config with all required vars
vastCSIEndpoint: "https://vms.example.com"
vastCSIAdminUsername: admin
vastCSIAdminPassword: password
vastCSIVipPool:
  subnet_cidr: 24
  ip_ranges:
    - start: 10.0.100.10
      end: 10.0.100.50
