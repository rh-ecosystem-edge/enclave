---
# Fixture: full valid defaults
vastCSIDefaults:
  infraTenant: infra
  storagePath: /osac
  validateCerts: true
  apiTimeout: 30
  apiVersion: latest
  csiProvisioners:
    nfs: csi.vastdata.com
    block: block.csi.vastdata.com
  csiDriverName: vastcsi-driver
  quayPvcSize: 1000Gi
  tiers:
    - name: quay
      protocol: nfs
